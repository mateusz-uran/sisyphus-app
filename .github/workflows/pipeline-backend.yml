name: Sisyphus backend API pipeline

on:
  push:
    branches:
      - ci/cd
    paths:
      - backend/**
      - 'docker-compose.yml'
      - .github/workflows/*-backend.yml

jobs:

  add-permissions:
    runs-on: ubuntu-latest
    name: Add executable permissions to maven
    steps:
      - name: Add permission
        run: chmod +x backend/mvnw

  compile:
    runs-on: ubuntu-latest
    needs: [add-permissions]
    name: Compile project
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Unit tests
        run: |
          cd backend
          ./mvnw clean compile

  unit-test:
    runs-on: ubuntu-latest
    name: Unit tests
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Run unit tests
        run: |
          cd backend
          ./mvnw clean test


