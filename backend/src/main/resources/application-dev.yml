spring:
  data:
    mongodb:
      uri: mongodb://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@localhost:27017/sisyphus-db?authSource=admin
application:
  cors:
    origins: http://localhost:4200, http://localhost:9090

server:
  port: 8080